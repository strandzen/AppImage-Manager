# Tasks

- [ ] Planning & Design [x]
    - [ ] Research AppImage update mechanisms [x]
    - [ ] Design packages.json update strategy [x]
    - [ ] Create implementation plan [x]
- [x] Implement Core Logic
    - [x] Add versioning to packages.json
    - [x] Implement Updater class in `logic.py`
    - [x] Add remote JSON fetching and comparison
    - [x] Add AppImage release checking via GitHub API
- [x] Implement UI Components
    - [x] Add update check feedback in `gui.py`
    - [x] Create update notification/dialog
- [x] Verification
    - [x] Test JSON comparison with a mock remote
    - [x] Verify GitHub API calls
    - [x] Document final update process
- [x] Previews Restructuring
    - [x] Move .md files to Markdown/
    - [x] Move .png files to Images/
    - [x] Update logic.py for new directory structure
- [x] UI & Logic Refinements
    - [x] Move system update checks to 'Manage' tab button
    - [x] Remove strikethrough and '(Installed)' tag in 'Manage' tab
    - [x] Synchronize item display between tabs
    - [x] Strip 'Install ' prefix in Manage tab
- [x] Consolidated Update UI
    - [x] Create ConsolidatedUpdateDialog
    - [x] Synchronize worker results in MainWindow
    - [x] Present unified results with links and app updates
- [x] Progress Feedback Redesign
    - [x] Add `progress_signal` to `InstallWorker`
    - [x] Add `QProgressBar` to `TerminalWindow`
    - [x] Implement Detail Toggle in `TerminalWindow`
    - [x] Handle task-by-task progress updates
- [x] Hidden Test Entry (test1)
    - [x] Add "Simulation" entry to `packages.json`
    - [x] Update `gui.py` search logic to reveal hidden entries
    - [x] Verify countdown logic in terminal
- [x] Preview Icon Formatting
    - [x] Rename `icon`, `markdown`, `image` folders
    - [x] Resolve resource discovery regression (use stored metadata)
    - [x] Apply `<img>` tag and `<br clear="left">` to headers
    - [x] Update `logic.py` for new directory names
- [x] Flathub ID Standardization
    - [x] Build Name -> ID mapping from `packages.json`
    - [x] Batch rename `markdown/*.md` and `image/*.png` to IDs
    - [x] Update `logic.py` to fetch previews by ID
    - [x] Integrate icons into list view sidebar
    - [x] Verify discovery for both Flatpaks and custom scripts
- [x] Sidebar UI Fixes
    - [x] Standardize all IDs and resource filenames to lowercase
    - [x] Correct "Hidden" category concealment in GUI
    - [x] Verify icon visibility for all apps (Brave, Chrome, etc.)
    - [x] Fix Bitwarden and Mission Center icon mismatches
    - [x] Add new apps for provided icons (LibreOffice, Zed, etc.)
- [x] PascalCase & Metadata Refactor
    - [x] Create `app_metadata.json` with settings and PascalCase names
    - [x] Simplify `packages.json` (remove names)
    - [x] Update `logic.py` to handle metadata and alphabetical sorting
    - [x] Update `gui.py` for configurable icon size and new naming
    - [x] Verify sorting and naming in UI
    - [x] Cleanup redundant name fields in `packages.json`
- [x] Code Review & Optimization
    - [x] Remove redundant `fetch_flatpak_names` (speed up startup)
    - [x] Clean up unused imports in `logic.py` and `gui.py`
    - [x] Simplify `filter_apps` logic in `gui.py`
    - [x] Improve code comments and docstrings
- [/] Automatic Resource Integration
    - [x] Create `get_app_resources` in `logic.py` for centralized discovery
    - [x] Update GUI to use automated local discovery
    - [-] Implement remote icon/description fetching (Reverted by user request)
- [x] Document Other Apps Locally
    - [x] Fetch READMEs for Ferdium, Konsave, LinNote, Theme-Switcher
    - [x] Create local `.md` files in `previews/markdown/`
    - [x] Remove `github_repo` links from `packages.json`
