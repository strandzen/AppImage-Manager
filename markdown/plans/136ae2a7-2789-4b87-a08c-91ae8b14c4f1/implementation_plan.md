# Fix Flatpak Command Transformations

The goal is to ensure `flatpak uninstall` and `flatpak update` commands generated by the app do not include the "flathub" remote name, as those commands treat it as an invalid application ID.

## Proposed Changes

### Logic Improvement in [logic.py](file:///home/herman/Documents/Project/Installer/logic.py)

#### [MODIFY] [logic.py](file:///home/herman/Documents/Project/Installer/logic.py)
- Update `get_remove_command` to remove "flathub" from the resulting uninstall command.
- Update `get_upgrade_command` to be more robust when stripping the installation prefix.

## Verification Plan

### Automated Tests
- I will run a script to test the `get_remove_command` and `get_upgrade_command` methods manually with sample inputs to verify correct output.

### Manual Verification
- None required from user, I will verify the logic internally.
