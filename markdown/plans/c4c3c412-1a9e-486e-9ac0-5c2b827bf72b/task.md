# Tasks

- `[x]` 1. Build & Dependency Setup
    - `[x]` Add `Qt6::Svg` dependency to root `CMakeLists.txt`
    - `[x]` Link `Qt6::Svg` in `src/CMakeLists.txt`
- `[x]` 2. Core Library Improvements
    - `[x]` Refactor `isNewerVersion()` in `appimageinfo.h` to use native `QVersionNumber`
    - `[x]` Modify `appimagemanager.cpp` to replace hardcoded paths with standard `QStandardPaths::writableLocation()`
    - `[x]` Implement robust `QSqlDatabase` connection cleanup in `appimagecache.cpp`
    - `[x]` Refactor `appimagereader.cpp` to extract accurate XDG `appId` from `.desktop` basename
    - `[x]` Implement self-extraction fallback in `appimagereader.cpp` using `QProcess` inside `QTemporaryDir`
- `[x]` 3. GUI & Daemon Logic Refactoring
    - `[x]` Integrate `QSvgRenderer` in `appimageiconprovider.cpp` to render vector SVG icons natively
    - `[x]` Refactor `UpdateDaemon` in `updatedaemon.cpp` to use `GitHubReleaseChecker` instead of duplicating network check logic
    - `[x]` Rename shadowed `sortRole`/`setSortRole` to `sortField`/`setSortField` in `appimagesortfiltermodel.h/.cpp`
    - `[x]` Update `AppSettings::openFolderPicker()` to accept a `QWindow*` parent for proper Wayland modal behavior
- `[x]` 4. QML & UX Polish
    - `[x]` Fix Master-Detail pane layout in `DashboardWindow.qml` with a stable list pane width
    - `[x]` Update references from `sortRole` to `sortField` in `DashboardWindow.qml`
    - `[x]` Pass standard window parent from QML settings dialog to C++ `openFolderPicker()`
- `[x]` 5. Verification & Testing
    - `[x]` Verify successful compilation with updated CMake configuration
    - `[x]` Manually test fallback extraction, SVG crisp rendering, settings dialog, and dashboard layout
